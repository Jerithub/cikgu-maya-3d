/** @type {import('tailwindcss').Config} */
export default {
  content: ["./index.html", "./src/**/*.{js,ts,jsx,tsx}"],
  // Tailwind v4: All custom theme values moved to src/index.css @theme directive
  // This config now only contains content paths
}
